# Margin runs with a deterministic mock provider when no keys are set.
# Set any of these to route real traffic (see server/providers/router.js).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
DEEPSEEK_API_KEY=
PORT=8787
MARGIN_DB=margin.db
