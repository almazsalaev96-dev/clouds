{
  "name": "slate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Slate: the static study app in web/, and the gateway in server/ deployed as one function under api/.",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  },
  "devDependencies": {
    "@types/node": "22.7.5",
    "typescript": "5.6.3"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
