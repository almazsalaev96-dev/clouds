{
  "name": "@slate/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Slate in the browser: the gateway's own grader and the reference learning engine, bundled into one HTML file that runs on an iPad with no server and no credentials.",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "node --experimental-strip-types --test test/*.test.js",
    "smoke": "node --experimental-strip-types tools/smoke.mjs",
    "check": "npm run build && npm test && npm run smoke"
  },
  "devDependencies": {
    "playwright": "1.48.2"
  }
}
