# Resolution log — v3 revision pass

One row per critique finding touched. Editors append; nothing is removed.

## §05, §06 (file `draft/05-06.md`)

| Finding | Action | File | What changed |
|---|---|---|---|
| c4-C4-03 | fixed | draft/05-06.md | Wrote **§05.13 The machine-scored and IRT engine** (05-IRT-001…014): eligible item families (deterministic key only, everything else raises `IrtIneligible`), no `per_ao` on an IRT Mark, distractor-to-misconception feedback in place of spans, 2PL default / bounded-prior 3PL, staged calibration volumes (300 trial→provisional, 500 live), infit/outfit 0.7–1.3 + point-biserial + Mantel–Haenszel DIF with automatic retirement, EAP θ with SE and a mandatory score *range*, the two blocking error gates (SE(θ) ≤ 0.30 at the cut scores; score RMSE ≤ the exam's published SEM), exposure control and trial seeding, item security, announced recomputation, and engine isolation. Added the matching **row to the 05-GATE-002 table** ("Machine-scored (IRT) score estimate", `human_line: "n/a"`) and the IRT field set to 05-GATE-008 via 05-IRT-013. §05.13 was the build's only dangling cross-reference; 05-SCOP-006 and §00/§02's citations already pointed here, so no citation needed repointing inside these files. |
| c1-F38 | fixed | draft/05-06.md | 05.12(a) `application_check` was `"tested": 5, "passed": 3` — a count with an unrepresented third pass. Now `"tested": 4` with `passed[]` and `failed[]` both arrays of span refs carrying offsets, so `len(passed)+len(failed)==tested` holds as 05-MARK-006 requires. Added `data_reference_check: null` (the Item is `case_study`, not `data_response`). |
| c1-F04 / c1 C3 (residue) | fixed | draft/05-06.md | The 05.12(a) "checks a builder should read off this Mark" paragraph still said "AO4 is capped at Level 1 by `command_word_check.met=false`", contradicting the rewritten 05-MARK-005/05-PROC-005. Rewritten as a bulleted check list: AO4 is Level 1 **from its own grid** (unsupported judgement + a weighing sentence that decides nothing); the command word sets `ceiling_ao` and `uncapped_level: 2` only, and the Mark now records `cap_binding: false`. The `coaching_line` was re-cut to the 05-MARK-005 template ("Evaluate — a judgement is required. None committed, so AO4 cannot reach Level 3.") in the JSON and in the rendered block. |
| c1-F20 (residue) | fixed | draft/05-06.md | The rendered AO1 line still read "the band is met without margin" — the "margin" vocabulary 05-SCHM-004 replaced with `within_band_rule`. It now names both credited spans with their line refs and states the `within_band_reason` verbatim. Added `within_band_reason` to AO3 and AO4 (AO1/AO2 already had it), which 05-SCHM-004 requires on every `per_ao` object. |
| c1-F03 / c3-F02 / c3-F04 / c4-C4-19 (fixtures) | fixed | draft/05-06.md | Both 05.12 `provenance` blocks were regenerated against the 05.2 contract: `model_family`→`family`+`model`+`effort`; `policy_version` is now the dated semver `2027.08.1` with per-paper marker identity in `marker_instructions_version`; `temperature` deleted (not settable, 08-ENS-003); `reproducibility_run` and `self_consistency{orderings, per_ao_medians}` present separately per 05-PIPE-006. Added the `required` fields the printed instances were missing: `created_at`, `annotations`, `challenge_state`, `procedure_used`, `data_reference_check`, `total_band.exact`, `calibration_status.last_audit`. |
| c1-F18 / c1 C6 | fixed | draft/05-06.md | 05.12(b) K1 carried `NOT_IN_INDICATIVE_VALID_ALT` on `awarded: 0`, which 05-MARK-003 now forbids and whose acceptance line names this very fixture. K1 is now `ATOM_NOT_MET`. |
| c1-F21 | fixed | draft/05-06.md | 05.12(b) K2's atom "attributed to customisation, one-off working or skilled labour" was `met: true` against a quote containing no attribution. The K2 span now runs to L3 (`char_start` 51 → `char_end` 192, verified against the printed transcript) and overlaps AN1; the scheme row declares `shareable: true, shareable_with: ["AN1"]` and both points echo `shared_with`, so 05-SCHM-009 licenses the overlap instead of the engine double-counting silently. |
| c1-F22 | partially fixed | draft/05-06.md | The fixture's `ao_split {AO1: 2, AO3: 3}` contradicted 05-GEN-010, which rejected any item omitting an AO weighted >20%. Rather than re-cut the fixture (which would also invalidate the §15.B.2 template that models it), 05-GEN-010 was split into the two rules c1's own "Why" argues for: **per item**, the split must sum to `tariff` and be legal under the section's `ao_split_rule` — a P1 Section A short answer with no insert genuinely offers no application; **per generated set**, the marks offered on each AO must sit within ±5 points of the paper's `ao_weights` share, rejecting the whole batch otherwise. 05.12(b) now states why its split is legal and where the paper's AO2 share comes from. Declined the re-cut to (a) [2] + (b) [3]: 9609 P1's section structure is unverified under 15-OPEN-010 and 05-GEN-001 forbids printing an unverified shape. |
| c1-F26 | declined — blocked upstream | draft/05-06.md | The 9609 P3 30-mark family (grid, AO split, legal generator combination, worked example) cannot be written: its structure is on the unverified list (15-OPEN-010) and §06.3 and §15.H ship two different shapes for it. 05-GEN-001 already refuses to generate for a paper whose `question_types` is empty or `verified_from_syllabus: false`, so the paper ships feedback-only (05-GATE-004) and the readiness dashboard names the missing fact. The ≥30 tolerance row exists and is generic. Writing a grid now would hard-code one of two guesses, which is what c3-F41 asks us not to do. |
| c3-F09 / c3 C1 | fixed | draft/05-06.md | 06-STOR-005 stated "12 months (school tenants may set 1–36 months)" against §11.13's 30-day transient ceiling, and the 06.2 matrix restated the 12 months. §06 now states **no retention number at all**: `uploads.retention_until` is computed from `config/retention.yaml` (§07.8 defaults, 11-PRIV-004 ceilings) and a duration literal in §06 is a docs-lint failure. What §06 keeps is the 24 h propagation rule, which is genuinely its own. |
| c3-F10 / c3 C9 | fixed | draft/05-06.md | The three-member provenance enum is restored and every refinement moved to a sibling column, stated once in §06.1: `consent_status ∈ {none, gold_consented}` (the §06.3 exemplar is now `provenance: USER, consent_status: gold_consented`, not `USER→gold_consented`); `text_status ∈ {own, own_facts, licensed, ref_only}` with `own_facts` added to the ledger enum; `licence_basis` including the new `quotation`; and `links[].derivation` replacing `edges[].provenance` (06-PACK-006 now says why). The 06.2 matrix's invented class "OWN-quotation" is gone from both rows. Added a lint that rejects any provenance literal containing `→`, `-` or `:`. |
| c3-F29 | fixed | draft/05-06.md | `quotation` added to `licence_basis` with the required `quotation_basis {exception ∈ {uk_cdpa_s30, us_fair_use, eu_art5_quotation, other}, jurisdiction, reviewed_by, reviewed_at}` sub-object on the ledger, and the acceptance test that every `extract_log` row resolves to a document whose basis is `licensed`, `public_with_permission` or `quotation` — so the command-word definitions and every ≤40-word extract stop being recorded as `own`. |
| c3-F30 | fixed | draft/05-06.md | New **06-STOR-010**: the command-word table is rendered as `definition_display` (our paraphrase) everywhere; the verbatim `definition` appears only in the detail popover, one word at a time, attributed, chip-linked and logged per view, never more than three per view and never as a table. The marker's static prefix is explicitly excluded (a context payload is not a display). The Pack bundle now carries both fields. Whole-table reproduction stays on the counsel list (15-OPEN-040). |
| c4-C4-22 (§06 half) | fixed | draft/05-06.md | New **06-STOR-011** fixes what a logged-out surface may render — anchors and metadata only, zero extracts, zero question/scheme/ER text, zero OWN Items, zero USER content, `display_rights` forced to `anchors_only` in the prefilter rather than in the page, non-affiliation notice and mirror blocklist unchanged — with a logged-out crawl as the acceptance. The screen itself (§10) and the §03.2 parity row are the other editors'. |
| c3-F03 / c3 C3 | fixed | draft/05-06.md | The §06.3 canonical bundle was rewritten field-for-field against `Pack@2`: `pack_version` in `version_tag` form with a derived `pack_version_display`; added `content_hash`, `compiled_at`, `allowed_domains`, `mark_scheme_item_ids`, `option_thresholds`, `documents`; `assessment_objectives` → `syllabus.aos[]` with `weight_overall`; `version_window` → `version_years`; `thresholds` flattened to one row per grade with `min_raw`; `question_types` re-shaped to `{section, tariffs, command_words, ao_split_rule}`; `syllabus_graph.{nodes,edges}` → top-level `syllabus_nodes` + `links`; misconceptions expanded to the schema's object form; `readiness` split into the enum string plus `readiness_evidence`; per-paper state moved to `papers[].twin_state`. 06-PACK-001 now names `Pack@2` (`schemas/pack@2.json`) as the single normative type source, kills the third filename `pack.v1.json`, lists the additive keys, fixes snake_case enums, and adds the CI job that validates every YAML/JSON block against the `$id` it claims. |
| c3-F05 | fixed | draft/05-06.md | One stored form for `pack_version` (`9609:2026-2028@2026.11.2`) with `pack_version_display` ("2026–28 v3") derived for banners; the changelog row and the migration note now use both correctly. |
| c3-F24 / c3 C10 | fixed | draft/05-06.md | 06-PACK-004 rewritten: verbatim board text lives in exactly two places — `command_words[].definition` and, **under licence only**, `aos[].definition` with `wording_status: licensed_verbatim`. AO definitions default to `own_paraphrase` with a `source_ref`, and the bundle shows them that way, so `Pack@2`'s required `definition_verbatim` no longer forces a second unlicensed verbatim site. The 8-gram overlap check now names `aos[].definition` and `command_words[].definition_display`. |
| c3-F27 | partially fixed | draft/05-06.md | §06.3's `series: "M/J"` + `year: 2025` split is merged into one two-digit field (`series: "M/J/25"`) in `questions[]` and `thresholds[]`, matching 06-PACK-002's regex. The four-digit, over-permissive pattern in `Item@2.question_ref.series` is §15.C.3's to fix; 06-PACK-002 is now the only place the grammar is stated. |
| c3-F41 / c3 C16 | fixed | draft/05-06.md | 9609 P3's `question_types` is now `[]` with `verified_from_syllabus: false`, `twin_state: not_yet_calibrated` and `markable: feedback_only`, so no agent hard-codes either of the two contested shapes. Every paper carries `verified_from_syllabus: false` until 15-OPEN-010 closes at M1 — that is what the open question actually says — with a comment that the printed numbers are the pre-read working shape and generate nothing under 05-GEN-001. |
| c3-F28 (§06 half) | fixed | draft/05-06.md | `gold-seeded` normalised to `gold_seeded` in §06.8's prose and 06-TEN-005; `expert_marked` kept snake_case; 06-PACK-001 states the convention and names the lint. `Pack@2`'s hyphenated literals and its missing `provisional` `twin_state` value remain §15.C.7's to fix. |
| c1-F40 (Pack half) | fixed | draft/05-06.md | `papers[].markable` and `papers[].verified_from_syllabus` are now actually present in the Pack bundle, and new **06-PACK-011** makes them Pack data feeding 05-SCOP-010's published list, the Course setup sheet and the calibration page. |
| c3 "missing: Pack version change mid-Course" (§06 half) | fixed | draft/05-06.md | `node_aliases[{from, to, relation ∈ {same, split, merged, removed}, since_version}]` added to the bundle and to 06-PACK-008, which now also states that prior Marks and Receipts are never re-marked or re-signed. This is the field 11-DATA-008 reads. |
| c3-F26 | partially fixed | draft/05-06.md | The §06.1 OWN example no longer presents the slug `it_9609_5.3_0412` as an id; the example is a ULID and the slug is called out as not an id. `questions[]` now carries both the canonical reference `id` and the ULID `item_id` it joins on, which is 06-PACK-002's "reference, never a primary key" made concrete. The §08.12 log row is §08's. |
| c3-F31 | fixed | draft/05-06.md | 06-TAKE-001's hard-coded `copyright@margin.study` is now `copyright@{brand_domain}` rendered from `config/brand.yaml`, with 01-NAME-001's PROVISIONAL status and the no-literals lint named. |
| c3-F32 | fixed | draft/05-06.md | Added **06-TAKE-009** (register a US Copyright Office DMCA designated agent before the first public build, keep the directory entry current, publish it at `/legal/notice`; the release checklist blocks on the registration number) — the number was unused — and **06-TAKE-015** (a statement of reasons to the uploading principal within 24 h of any USER-content removal: ground, facts, automated-means flag, territorial scope, redress routes, plus DSA Transparency Database submission where it applies). 06-TAKE-020's transparency page now counts both. |
| c3-F21 / c3 C12 | fixed | draft/05-06.md | §06.8's 9609 cell claimed P4 is calibrated first while §14.3 gates P2 first. Rewritten: gating order inside a Pack follows §14.3 (P2, then P1, then P4 at M12, provisional at launch); choosing 9609 first buys the hardest *paradigm* early, not the hardest paper first. |
| c4-C4-09 (§06 half) | fixed | draft/05-06.md | The T1 tier row said "Trivial; no gold scripts needed", which no longer matches the 05-GATE-002 exact-match row it must agree with. It now reads "a 200-response key-audit set; no double-marked scripts, because the human line is not applicable". |
| c4-C4-06 (§05/§06 half) | fixed | draft/05-06.md | `pack.v1.json` and `mark_scheme_item.v1.json` replaced by `Pack@2` and `MarkScheme@3` in 06-PACK-001, compiler stage 16, the 06.7 prompt heading and the 06.7 validation table; 06-PACK-001 states that the `Name@n` `$id` is the only legal `schema_id`. |
| c4 contradiction 11 (second half) | fixed | draft/05-06.md | IB criteria coaching had no specification anywhere, though §14.2 and 05-SCOP-006 both point at §05 for it. New **05-SCOP-011** writes it as a fourth *mode* on the levels engine, not a fourth engine: never a mark (`total_band = null`, `per_ao` carries `criterion_id` with `level: null`), teacher-marks-first read-only state where a Class exists, mandatory draft-feedback mode with a Receipt entry per Turn, and criteria descriptors as `own_paraphrase` unless licensed. 05-SCOP-006 now cites it. |
| c3-F35 (§06 half) | fixed | draft/05-06.md | The §06.2 per-region row now says what DeepSeek may not receive on the *content* side, not just the PII side: no Pack-derived OWN text — no `mark_scheme_items`, no `examiner_insights`, no generated Items, no grids — because the provider carries no contractual no-training assertion; permitted payloads are public question stimuli and the numeric pre-solve task. The route-level rule (08-POL-011) stays §08's. |
| c1-F33 | partially fixed | draft/05-06.md | The Pack is now declared the only source of syllabus numbering for prose, fixtures and screens, and 06-PACK-005 adds the docs-and-fixtures lint that resolves every `syllabus_point_id` and every "{path} {title}" string in the repo against the test Pack. The §07.5 / §10.6 / §04.15 strings themselves are those sections' to correct; the lint is what stops them recurring. |
| c1-F17 / c4-C4-13 (residue) | fixed | draft/05-06.md | 05-CHAL-002 banned the word "upheld" "anywhere in this document" while 06-TAKE-012 legitimately says "three upheld notices". The ban is now scoped to the marking-challenge vocabulary and names the one surviving unrelated sense, so the copy lint can be written without a false positive. |
| c1-F03 / c1-F04 / c1-F08 / c1-F09 / c1-F15 / c1-F16 / c1-F27 / c1-F28 / c1-F30 / c1-F31 / c1-F36 / c1-F39 / c1-F40 / c2-C2-14 / c4-C4-15 / c4-C4-28 / c4-C4-13 / c1 missing (MCQ gate row, essay-plan Item, in-response time allocation, practical schemes, within-band metric, AO independence, "what we don't mark") | verified — already resolved in the draft | draft/05-06.md | Spot-checked each against the text before touching anything, per the resumed-pass rule: 05-PIPE-006 already splits the reproducibility run from the ordering-varied ensemble; 05-MARK-005/05-PROC-005 already delete the AO4 cap; 05-GATE-001a/b already count gradable responses with the ≥50-per-family floor and CI-lower-bound gating; 05-MARK-009 already sums per-AO medians; 05-SCHM-004 already carries `within_band_rule`; §05.3 already holds the single tolerance table; 05-MARK-015 already adds `data_reference_check`; 05-GATE-005 already uses CUSUM + SPRT; 05-GATE-002b already gates fairness on the CI at n≥100; 05-PIPE-003 already keeps `seen_solution` out of `mark@`; 05-TIME-002 already reads `minutes_per_mark` from the Pack; 05-SCOP-010, 05-MARK-016, 05-GATE-010, 05-GEN-011, 05-TIME-011, 05-SCHM-009 and 05-SCHM-010 already exist. No fix was applied twice. |
| c1-F01/F02/F05/F06/F07/F10–F14/F19/F23/F24/F25/F29/F32/F34/F35/F37; c2 all; c3-F01/F02/F04/F06/F07/F08/F11–F20/F22/F23/F25/F33–F40/F42/F43; c4-C4-01/02/04/05/07/08/10/11/12/14/16–18/20/21/23–27/29–35 | declined — out of section | — | These name §00–§04, §07–§15. Where one touches §05/§06 by reference — retention, the DeepSeek content rule, the Pack schema, the takedown report, the calibration-page fields, the IRT gate row — the §05/§06 side is fixed above and the owning section is named in the text. c4-C4-04's cross-reference sweep is clean for these files: after §05.13 was written, `stitch.py` reports zero dangling subsection references across the whole build. |
